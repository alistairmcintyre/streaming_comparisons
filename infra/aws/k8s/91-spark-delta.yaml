# Spark Delta streaming pipeline (Kafka → Delta on S3). Four long-running
# SparkApplications; each self-provisions its tables (ensure_all → createIfNotExists,
# jobs/_shared/delta_tables.py) so there's no ordering dependency. COMPACTION is
# IN-pipeline (Optimized Writes + Auto Compaction via table props and session confs)
# and mirrors Paimon self-compaction; only VACUUM is a separate job (93-maintenance.yaml).
# S3 via IRSA, with the DynamoDB LogStore for commit mutual exclusion.
# REQUIRED, not optional: S3 has no atomic put-if-absent, so the default
# S3SingleDriverLogStore only makes commits safe from one driver. VACUUM runs as a
# SEPARATE SparkApplication and does commit (verified on Delta 4.0.0: it writes
# VACUUM START + VACUUM END versions), so there are genuinely two committers per
# table. The image carries the AWS SDK v1 jars this LogStore needs, see
# docker/spark-delta/Dockerfile. Keep this in sync with 93-maintenance.yaml.
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: delta-bronze-trades, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-delta:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/bronze_trades.py"
  sparkVersion: "3.5.1"
  # BOUNDED RESTARTS. `type: Always` retries forever, so a deterministically-failing job
  # (bad schema, missing credential, unreadable table) crash-loops indefinitely while
  # still reporting as deployed, burning cluster and looking healthy. `OnFailure` with a
  # retry cap lets transient failures self-heal (each restart resumes from the streaming
  # checkpoint) but lets a permanent failure actually FAIL, where the PodCrashLooping
  # alert in 96-alerts.yaml can surface it.
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # 8, not Spark's default of 200: these executors run 1 core, so 200 shuffle
    # partitions is 200 serialised tasks per stage, and AQE cannot coalesce them
    # because Spark disables AQE for streaming queries. Tidying rather than a fix,
    # measured at 3% against real S3.
    "spark.sql.shuffle.partitions": "8"
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension"
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog"
    "spark.databricks.delta.optimizeWrite.enabled": "true"
    "spark.databricks.delta.autoCompact.enabled": "true"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
    "spark.delta.logStore.s3a.impl": "io.delta.storage.S3DynamoDBLogStore"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.tableName": "${DELTA_LOGSTORE_TABLE}"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.region": "${AWS_REGION}"
  volumes:
    - { name: jobs, configMap: { name: spark-delta-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    memory: "2g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: delta-silver-trades, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-delta:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/silver_trades.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # 8, not Spark's default of 200: these executors run 1 core, so 200 shuffle
    # partitions is 200 serialised tasks per stage, and AQE cannot coalesce them
    # because Spark disables AQE for streaming queries. Tidying rather than a fix,
    # measured at 3% against real S3.
    "spark.sql.shuffle.partitions": "8"
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension"
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog"
    "spark.databricks.delta.optimizeWrite.enabled": "true"
    "spark.databricks.delta.autoCompact.enabled": "true"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
    "spark.delta.logStore.s3a.impl": "io.delta.storage.S3DynamoDBLogStore"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.tableName": "${DELTA_LOGSTORE_TABLE}"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.region": "${AWS_REGION}"
  volumes:
    - { name: jobs, configMap: { name: spark-delta-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    # 3g, not 2g. At 2g the pod limit is 2867Mi (heap x Spark's 0.4 non-JVM factor)
    # and executors were OOMKilled at exactly that on a live run, 16 of them on this
    # job alone.
    # HEADROOM, NOT A DIAGNOSIS, and the distinction is worth keeping. Three theories
    # have now been measured against the real 2867Mi cgroup limit with
    # tests/executor_memory_soak.py, and none reproduces the kill on its own:
    #
    #   state size          9.4M rows through a 2h window   peak 1375MiB   survived
    #   rocksdb unbounded   boundedMemoryUsage on vs off    no useful difference
    #   200 shuffle parts   833 SST files vs 40 at 8        peak 1617MiB   survived
    #
    # The third is the one that matters and it is a CONTRIBUTOR rather than the cause.
    # Spark creates one RocksDB state store instance PER SHUFFLE PARTITION per stateful
    # operator, each allocating its own memtables and block cache off-heap, so the 200
    # this deployment ran until recently cost about 370MiB of native memory over the 8 it
    # runs now: 30% of the peak, invisible to every JVM heap setting. That was shipped as
    # a 3% latency tidy-up and is worth considerably more than it was sold as.
    # What the soak still does not carry, and any of it could close the remaining gap: a
    # separate executor JVM, S3A upload buffers, an inline-compacting Delta write, PySpark
    # worker processes, and a sustained Kafka backlog rather than a replayed topic.
    # Shortening the watermark to 1h (jobs/spark-*/silver_trades.py) halves the state and
    # remains the better-evidenced half of this change.
    memory: "3g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: delta-silver-accounts, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-delta:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/silver_accounts.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # 8, not Spark's default of 200: these executors run 1 core, so 200 shuffle
    # partitions is 200 serialised tasks per stage, and AQE cannot coalesce them
    # because Spark disables AQE for streaming queries. Tidying rather than a fix,
    # measured at 3% against real S3.
    "spark.sql.shuffle.partitions": "8"
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension"
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog"
    "spark.databricks.delta.optimizeWrite.enabled": "true"
    "spark.databricks.delta.autoCompact.enabled": "true"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
    "spark.delta.logStore.s3a.impl": "io.delta.storage.S3DynamoDBLogStore"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.tableName": "${DELTA_LOGSTORE_TABLE}"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.region": "${AWS_REGION}"
  volumes:
    - { name: jobs, configMap: { name: spark-delta-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    # 3g for the same reason as silver-trades: the diagnostics bundle attributed all
    # 20 OOMKilled executors of a live run to the silver jobs (16 delta trades, 3
    # iceberg trades, 1 delta accounts), and delta-silver-accounts shows the full
    # chain: OOMKilled -> SparkExecutorFailed -> SparkDriverFailed -> PendingRerun.
    memory: "3g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: delta-gold-open-positions, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-delta:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/gold_open_positions.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # 8, not Spark's default of 200: these executors run 1 core, so 200 shuffle
    # partitions is 200 serialised tasks per stage, and AQE cannot coalesce them
    # because Spark disables AQE for streaming queries. Tidying rather than a fix,
    # measured at 3% against real S3.
    "spark.sql.shuffle.partitions": "8"
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension"
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog"
    "spark.databricks.delta.optimizeWrite.enabled": "true"
    "spark.databricks.delta.autoCompact.enabled": "true"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
    "spark.delta.logStore.s3a.impl": "io.delta.storage.S3DynamoDBLogStore"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.tableName": "${DELTA_LOGSTORE_TABLE}"
    "spark.io.delta.storage.S3DynamoDBLogStore.ddb.region": "${AWS_REGION}"
  volumes:
    - { name: jobs, configMap: { name: spark-delta-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    memory: "2g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: DELTA_WAREHOUSE, value: "s3a://${WAREHOUSE_BUCKET}/delta" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }

