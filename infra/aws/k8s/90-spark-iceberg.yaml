# Spark Iceberg streaming pipeline (Kafka → Iceberg on S3, Glue catalog) via the
# Spark Operator. Four long-running SparkApplications (bronze → silver → gold). Each
# job self-provisions its tables (ensure_all → CREATE IF NOT EXISTS, see
# jobs/_shared/iceberg_tables.py), so there is no ordering dependency. Jobs + shared
# modules come from ConfigMaps. Catalog `rest` is bound to Glue on AWS; S3 via IRSA.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lake
  namespace: spark
  annotations: { eks.amazonaws.com/role-arn: "${WORKLOAD_ROLE_ARN}" }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata: { name: spark-driver, namespace: spark }
rules:
  - apiGroups: [""]
    resources: [pods, services, configmaps, persistentvolumeclaims]
    verbs: ["*"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata: { name: spark-driver, namespace: spark }
subjects: [{ kind: ServiceAccount, name: lake, namespace: spark }]
roleRef: { kind: Role, name: spark-driver, apiGroup: rbac.authorization.k8s.io }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: iceberg-bronze-trades, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-iceberg:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/bronze_trades.py"
  sparkVersion: "3.5.1"
  # BOUNDED RESTARTS. `type: Always` retries forever, so a deterministically-failing job
  # (bad schema, missing credential, unreadable table) crash-loops indefinitely while
  # still reporting as deployed, burning cluster and looking healthy. `OnFailure` with a
  # retry cap lets transient failures self-heal (each restart resumes from the streaming
  # checkpoint) but lets a permanent failure actually FAIL, where the PodCrashLooping
  # alert in 96-alerts.yaml can surface it.
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.catalog.rest": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.rest.catalog-impl": "org.apache.iceberg.aws.glue.GlueCatalog"
    "spark.sql.catalog.rest.warehouse": "s3://${WAREHOUSE_BUCKET}/iceberg"
    "spark.sql.catalog.rest.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
  volumes:
    - { name: jobs, configMap: { name: spark-iceberg-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    memory: "2g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: iceberg-silver-trades, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-iceberg:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/silver_trades.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.catalog.rest": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.rest.catalog-impl": "org.apache.iceberg.aws.glue.GlueCatalog"
    "spark.sql.catalog.rest.warehouse": "s3://${WAREHOUSE_BUCKET}/iceberg"
    "spark.sql.catalog.rest.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
  volumes:
    - { name: jobs, configMap: { name: spark-iceberg-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    # 3g, not 2g. At 2g the pod limit is 2867Mi (heap x Spark's 0.4 non-JVM
    # factor) and this executor was OOMKilled at exactly that during a live run:
    # the 2h dropDuplicatesWithinWatermark state is on RocksDB (off-heap native),
    # and the Kafka buffers, S3A upload buffers and PySpark workers are off-heap
    # too, none of which a JVM heap setting bounds.
    memory: "3g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: iceberg-silver-accounts, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-iceberg:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/silver_accounts.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.catalog.rest": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.rest.catalog-impl": "org.apache.iceberg.aws.glue.GlueCatalog"
    "spark.sql.catalog.rest.warehouse": "s3://${WAREHOUSE_BUCKET}/iceberg"
    "spark.sql.catalog.rest.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
  volumes:
    - { name: jobs, configMap: { name: spark-iceberg-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    memory: "2g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
---
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata: { name: iceberg-gold-open-positions, namespace: spark }
spec:
  type: Python
  mode: cluster
  image: "${ECR_REGISTRY}/streaming-comparison/spark-iceberg:latest"
  # :latest is mutable, without Always, nodes reuse a stale cached layer
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/jobs/gold_open_positions.py"
  sparkVersion: "3.5.1"
  restartPolicy: { type: OnFailure, onFailureRetries: 10, onFailureRetryInterval: 10, onSubmissionFailureRetries: 5, onSubmissionFailureRetryInterval: 20 }
  sparkConf:
    # Keep the executor POD after it dies. Spark deletes it by default, so the one
    # place that records Reason=OOMKilled / Exit Code 137 is gone before
    # collect-diagnostics.sh runs -- which is exactly why ExecutorDeadException and
    # MetadataFetchFailedException came back from the last run unexplained.
    "spark.kubernetes.executor.deleteOnTermination": "false"
    # State store on DISK, not executor heap. silver.trades dedupes with
    # dropDuplicatesWithinWatermark over a 2h window, and the default
    # HDFSBackedStateStoreProvider holds every distinct trade_id in the window in JVM
    # heap, ~7.2M entries at 1k/s, against executors sized 4g. It also removes a
    # measurement asymmetry: the Flink engines already run state.backend=rocksdb, so
    # holding Spark's state on-heap would be a difference in the harness rather than
    # in the engines. Verified loading and surviving a restart by
    # tests/dedupe_state_test.py (27 rocksdb* metrics reported, state restored).
    "spark.sql.streaming.stateStore.providerClass": "org.apache.spark.sql.execution.streaming.state.RocksDBStateStoreProvider"
    "spark.sql.catalog.rest": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.rest.catalog-impl": "org.apache.iceberg.aws.glue.GlueCatalog"
    "spark.sql.catalog.rest.warehouse": "s3://${WAREHOUSE_BUCKET}/iceberg"
    "spark.sql.catalog.rest.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "software.amazon.awssdk.auth.credentials.DefaultCredentialsProvider"
  volumes:
    - { name: jobs, configMap: { name: spark-iceberg-jobs } }
    - { name: shared, configMap: { name: spark-shared } }
  driver:
    serviceAccount: lake
    cores: 1
    # k8s CPU *request* only, spark.driver.cores stays 1. The driver just
    # coordinates microbatches (executors do the work), so a full core of
    # scheduling footprint is wasteful on a quota-capped cluster.
    coreRequest: "500m"
    memory: "2g"
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }
  executor:
    instances: 1
    cores: 1
    memory: "2g"
    serviceAccount: lake
    env:
      - { name: KAFKA_BROKERS, value: "trades-kafka-bootstrap.kafka.svc.cluster.local:9092" }
      - { name: CHECKPOINT_BASE, value: "s3a://${WAREHOUSE_BUCKET}/_chk" }
      - { name: ICEBERG_CATALOG, value: "rest" }
      - { name: LATENCY_TOPIC, value: "pipeline_latency" }
      - { name: PYTHONPATH, value: "/opt/shared" }
    volumeMounts:
      - { name: jobs, mountPath: /opt/jobs }
      - { name: shared, mountPath: /opt/shared }

