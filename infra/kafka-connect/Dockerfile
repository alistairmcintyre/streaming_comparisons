FROM debezium/connect:2.5

# No additional plugins needed, debezium/connect:2.5 ships with PostgreSQL connector.
# Connectors are registered at runtime via the REST API (see scripts/register_connectors.sh).

USER 1001
